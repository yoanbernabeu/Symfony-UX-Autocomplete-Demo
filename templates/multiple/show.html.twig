{% extends 'base.html.twig' %}

{% block title %}Multiple{% endblock %}

{% block body %}
    <h1>Multiple</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ multiple.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ multiple.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_multiple_index') }}">back to list</a>

    <a href="{{ path('app_multiple_edit', {'id': multiple.id}) }}">edit</a>

    {{ include('multiple/_delete_form.html.twig') }}
{% endblock %}
