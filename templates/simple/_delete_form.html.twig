<form method="post" action="{{ path('app_simple_delete', {'id': simple.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ simple.id) }}">
    <button class="btn">Delete</button>
</form>
